node_modules/
dist/
.vercel/
*.tsbuildinfo
supabase/.temp/

.env
.env.*
!.env.example

*.log
.DS_Store

# Large source downloads are reproducible through the scripts in scripts/.
data/raw/
# Derived, 15 MB: the full FAA TAF annual series for all 3,319 facilities. Regenerate with
# `python3 scripts/parse-faa-taf-full.py` (reads the cached zip in data/raw/).
data/out/faa-taf-annual.json
.vercel

# Prompt text copied verbatim from a third-party site; not part of this project's work.
docs/promptforge-selected-prompts.md

# Hebrew-language versions of the docs (including the interview prep). Written for my own
# reading rather than for anyone else, and they duplicate content the English documents
# already carry, so the public repository keeps one language.
docs/*-he.html
docs/*-he.md

# Cover page written to accompany a copy of this project when it is handed to someone.
# It restates ARCHITECTURE.md and README.md for that one purpose, so it travels with the
# copy rather than living in the repository.
docs/SUBMISSION.md
docs/submission.html
